# UI Framework (System Tray)
PyQt6>=6.6

# Global hooks (Mouse events, fallback)
pynput>=1.7
python-dotenv
requests>=2.31.0

# Linux-only: kernel-level keyboard interception
evdev>=1.7; sys_platform == "linux"
