1. Needs to decide the behaviour when: a user types a word in one language, but the system thinks it's the other language, and then the user presses enter. Does the program needs to switch to the other language, or does it needs to stay in the current language? Either way, Enter press needs to go though. On the one hand, if a user pressed enter, and the tetx is wrong, we need to save him (convert before sending to the OS). On the other hand, if we switch language, and we shouldn't have, this is far more frastrating. This is basicaly choosing between false positives and false negatives. If my ability to make the decision is much better that it currently is than I would go for the switch.
2. Better data sources: currently the data has only subtitles which are spoken text (not typed text) and do not contain human typing errors.
    2.1 I can get some mixture of google searches, email adresses, youtube, facebook, StackExchange, wikipedia talk pages, Reddit, Twitter, etc. I might also source a "Golden Evaluation set" (key log all of my typing for a a couple of weeks) to tune the correct mixture of each source.
3. Add support for linux.
4. Let the user decide if pressing the pause button also changes language.